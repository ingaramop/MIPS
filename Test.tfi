


// Instantiate the module
Test instance_name (
    .clock(clock), 
    .r_data(r_data), 
    .rx_empty(rx_empty), 
    .sendSignal(sendSignal), 
    .rd_uart(rd_uart), 
    .sendData(sendData)
    );


