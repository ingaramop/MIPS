


// Instantiate the module
DebuggerRx_2 instance_name (
    .clk(clk), 
    .r_data(r_data), 
    .rx_ready(rx_ready), 
    .global_reset(global_reset), 
    .rd_uart(rd_uart), 
    .send_signal(send_signal), 
    .sendData(sendData)
    );


