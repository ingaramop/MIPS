


// Instantiate the module
Hazard_Unit instance_name (
    .memreadMEM(memreadMEM), 
    .rs_IFID(rs_IFID), 
    .rt_IFID(rt_IFID), 
    .rt_IDEX(rt_IDEX), 
    .muxCtrlSignal(muxCtrlSignal), 
    .IFIDWrite(IFIDWrite)
    );


