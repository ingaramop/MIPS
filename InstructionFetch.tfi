


// Instantiate the module
InstructionFetch instance_name (
    .clk(clk), 
    .reset(reset), 
    .PCJump(PCJump), 
    .jumpAdd(jumpAdd), 
    .PCSrc(PCSrc), 
    .writeIFID(writeIFID), 
    .opcjump(opcjump), 
    .Instruction(Instruction), 
    .PCNextReg(PCNextReg)
    );


